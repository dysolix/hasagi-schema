{
  "name": "@hasagi/schema",
  "version": "0.7.0",
  "scripts": {
    "build": "tsc && node scripts/postbuild.mjs",
    "check:package": "npm run build && publint ./dist && attw --pack ./dist",
    "publish": "npm run check:package && node scripts/prepublish.mjs && cd ./dist && npm publish --access public"
  },
  "keywords": [
    "hasagi"
  ],
  "author": "dysolix",
  "license": "MIT",
  "dependencies": {
    "@hasagi/core": "^0.6.3",
    "axios": "^1.7.9"
  },
  "devDependencies": {
    "@arethetypeswrong/cli": "^0.18.3",
    "@types/node": "^20.19.42",
    "prettier": "^3.5.3",
    "prompt": "^1.3.0",
    "publint": "^0.3.21",
    "typescript": "^6.0.3"
  },
  "exports": {
    ".": "./index.js"
  },
  "types": "./index.d.ts",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/dysolix/hasagi-schema.git"
  }
}
